steps:
  - unzip_top:
  - bss_gro_amb:
  # Due to instabilities, minimize after conversion
  - sander_mdrun:
      in:
        input_mdin_path: min.mdin
        output_rst_path: '&min.rst'
        output_traj_path: min.crd
  - npt_amber.yml:
  - bss_amb_gro:
      in:
        input_crd_path: '*amb.rst'
  - zip_top:
  # Due to instabilities, minimize after conversion
  - steep.yml: